# /etc/nginx/conf.d/dash-ratelimit.conf
# Rate-limit zone for BTC Bot dashboard.
# Must live in http{} context (conf.d/ is included there).
limit_req_zone $binary_remote_addr zone=dash_login:10m rate=5r/m;
